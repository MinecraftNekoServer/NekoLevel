# NekoLevel 配置文件

# 最大等级限制
max-level: 5000

# 数据库配置
database:
  host: "localhost"
  port: 3306
  database: "neko_level"
  username: "root"
  password: "wcjs123"

# 缓存配置
cache:
  # 缓存超时时间（毫秒）- 默认1小时 (3600000毫秒)
  timeout: 3600000